// Use this file for your unit tests.
// When you are ready to submit, REMOVE all using statements to your project (entities/controllers/etc)
namespace FestivalManager.Tests
{
    using FestivalManager.Core.Controllers;
    using FestivalManager.Entities;
    using FestivalManager.Entities.Sets;
    using NUnit.Framework;

	[TestFixture]
	public class SetControllerTests
    {
		[Test]
	    public void Test()
	    {
            //var stage = new Stage();
            //stage.AddSet(new Medium("Medium"));
            //stage.GetSet("Medium").AddSong(new Song("Song1", new System.TimeSpan(0, 2, 0)));

            //stage.GetSet("Medium").AddPerformer(new Performer("Evgeni", 15));

            //var setController = new SetController(stage);

            //var sb = new System.Text.StringBuilder();
            //sb.AppendLine("1. Medium:")


		}
	}
}